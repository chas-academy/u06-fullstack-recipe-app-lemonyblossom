/* import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RecipesMatchedComponent } from './recipesmatched.component'; */

/* describe('RecipesMatchedComponent', () => {
  let component: RecipesMatchedComponent;
  let fixture: ComponentFixture<RecipesMatchedComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [RecipesMatchedComponent]
    })
      .compileComponents();

    fixture = TestBed.createComponent.(RecipesMatchedComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
